To run this:

python predict.py

The base network VGG16 is in the folder Networks.

The sample images are locate in folder sample.

I know, the requirement file contains a lot of them but, at least it will work. 

pip install -r requirements.txt

have fun.
